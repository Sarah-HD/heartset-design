{
  "name": "User",
  "type": "object",
  "properties": {
    "cohort_type": {
      "type": "string",
      "enum": [
        "focus_group",
        "sprint",
        "advisory"
      ],
      "description": "User's program tier for conditional visibility"
    }
  },
  "required": [
    "cohort_type"
  ]
}